"""Vision utilities (screen capture, cursor tracking).

This package is optional: install with `pip install -e '.[vision]'`.

Modules avoid importing heavy deps at import-time where possible.
"""

from __future__ import annotations
